@{
    Layout = "~/Views/Layout/KYALayout.cshtml";
}

// действует, если вызов осуществляется через ViewResult
// не действует, если вызов через PartialViewResult
// если применяется ViewStart.cshtml, то сначала поиск в родном директории, а потом в shared 